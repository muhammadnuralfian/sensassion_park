Options -Indexes
RewriteEngine On

# URL tanpa .html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+?)/?$ $1.html [L]

# Default halaman admin = login
DirectoryIndex login.html
